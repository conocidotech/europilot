"""Europilot UI theme constants for the mici screens.

One place for the brand accent so the home wordmark and the onroad accents stay
in sync. Merge-safe: new file, imported where a colour is needed.
"""

import pyray as rl

# Europilot accent green (#1FC77D). Used for the home wordmark and onroad accents.
ACCENT = rl.Color(31, 199, 125, 255)
